input {
  tcp {
    port => 5044
    codec => json_lines
    host => "0.0.0.0"
  }
}

filter {
  # keep raw JSON as-is; add any parsing here
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "backend-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
